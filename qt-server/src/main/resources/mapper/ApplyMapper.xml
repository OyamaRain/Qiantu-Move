<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hotaru.mapper.ApplyMapper">

    <select id="selectByUserId" resultType="com.hotaru.entity.MoverApply">
        SELECT * FROM mover_apply WHERE user_id = #{userId}
    </select>

    <insert id="insert">
        insert into mover_apply
        (user_id, name, phone, apply_status, reject_reason, create_time, update_time)
        VALUES (#{userId}, #{name}, #{phone}, #{applyStatus}, #{rejectReason}, #{createTime}, #{updateTime})
    </insert>

    <update id="updateStatus">
        update mover_apply
        <set>
            <if test="applyStatus != null">
                apply_status = #{applyStatus},
            </if>
            <if test="rejectReason != null">
                reject_reason = #{rejectReason},
            </if>
            update_time = NOW()
        </set>
        where id = #{id}
    </update>

</mapper>